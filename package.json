{
  "scripts": {
    "clean:build": "rimraf dist",
    "clean:start": "rimraf web",
    "build": "npm run clean:build && parcel build index.js",
    "start": "npm run clean:start && parcel serve --dist-dir web index.html"
  },
  "devDependencies": {
    "parcel": "next"
  }
}
